{
  "name": "quiet-reader-extension",
  "version": "0.2.0",
  "private": true,
  "description": "A local-first browser reading extension for PDF, EPUB and MOBI books.",
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/patch-foliate.mjs && wxt prepare",
    "check": "npm run build:core && npm run typecheck && npm run typecheck:vue && node --check src/background.js && node --check src/reader.js && node --check src/annotation-export.js && node --check src/annotation-import.js && node --check src/text-anchor.js && node --check src/text-range.js && node --check src/open-errors.js && node --check src/storage-schema.js && node --check src/storage.js && node --check src/library-backup.js && node --check src/book-repository.js && node --check src/progress-service.js && node --check src/reader-adapter.js && node --check src/continuous-layout.js && node --check src/continuous-ebook.js && node --check scripts/create-boundary-epub.mjs && node --check scripts/patch-foliate.mjs",
    "test": "vitest run",
    "build:core": "tsc -p tsconfig.runtime.json",
    "typecheck": "tsc -p tsconfig.json",
    "sync:assets": "node scripts/sync-public-assets.mjs",
    "dev": "npm run sync:assets && wxt",
    "build": "npm run sync:assets && wxt build",
    "build:firefox": "npm run sync:assets && wxt build -b firefox",
    "zip": "npm run sync:assets && wxt zip",
    "typecheck:vue": "vue-tsc --noEmit -p .wxt/tsconfig.json",
    "fetch:test-books": "node scripts/fetch-test-books.mjs",
    "e2e:prepare": "npm run build:core && npm run fetch:test-books",
    "test:e2e": "npm run e2e:prepare && playwright test"
  },
  "dependencies": {
    "foliate-js": "1.0.1",
    "pdfjs-dist": "6.1.200",
    "pinia": "^4.0.2",
    "vue": "^3.5.40"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@vitejs/plugin-vue": "^6.0.8",
    "@vue/test-utils": "^2.4.11",
    "jsdom": "^30.0.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10",
    "vue-tsc": "^3.3.8",
    "wxt": "^0.21.1"
  }
}
