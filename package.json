{
  "name": "quiet-reader-extension",
  "version": "0.2.0",
  "private": true,
  "description": "A local-first browser reading extension for PDF, EPUB and MOBI books.",
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/patch-foliate.mjs",
    "check": "npm run build:core && npm run typecheck && node --check src/background.js && node --check src/reader.js && node --check src/storage.js && node --check src/book-repository.js && node --check src/progress-service.js && node --check src/reader-adapter.js && node --check src/continuous-layout.js && node --check src/continuous-ebook.js && node --check scripts/patch-foliate.mjs",
    "test": "vitest run",
    "build:core": "tsc -p tsconfig.runtime.json",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "foliate-js": "1.0.1",
    "pdfjs-dist": "6.1.200"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
